# Este archivo se usa cuando corres el backend FUERA de Docker (uvicorn local).
# Dentro de Docker Compose, las variables de entorno del docker-compose.yml tienen prioridad.

DATABASE_URL=postgresql+asyncpg://mujeres_user:mujeres_pass@localhost:5432/mujeres_db
SECRET_KEY=super-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_DAYS=30
